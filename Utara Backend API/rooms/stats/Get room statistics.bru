meta {
  name: Get room statistics
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/rooms/stats
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}

docs {
  Only SUPER_ADMIN and STAFF can access room statistics
}
