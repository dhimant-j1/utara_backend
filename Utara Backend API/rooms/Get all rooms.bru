meta {
  name: Get all rooms
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/rooms?floor=-77724087&type=SUITE&is_visible=false&is_occupied=true
  body: none
  auth: bearer
}

params:query {
  floor: -77724087
  type: SUITE
  is_visible: false
  is_occupied: true
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}

docs {
  - SUPER_ADMIN and STAFF can see all rooms
  - Regular users can only see visible rooms
  
}
