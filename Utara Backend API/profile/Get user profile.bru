meta {
  name: Get user profile
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/profile
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}
