meta {
  name: Scan a food pass
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/food-passes/scan
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
    "pass_id": "magna tempor aliqua nisi"
  }
}

settings {
  encodeUrl: true
}

docs {
  Only SUPER_ADMIN and STAFF can scan food passes
}
