meta {
  name: Get room requests
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/room-requests?status=REJECTED&user_id=aute veniam labore
  body: none
  auth: bearer
}

params:query {
  status: REJECTED
  user_id: aute veniam labore
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}

docs {
  - SUPER_ADMIN and STAFF can see all requests
  - Regular users can only see their own requests
  
}
